# class assignement

# create a dict and name the var d1 and add properties ->  name, age, gender
# update above dict using update function and add more information i.e salary, department, joining_date
# remove the key 'age' from the dict
# add new key "dob" in above dict
# overwrite above key "dob" with "date_of_birth". hint: store value of dob in a var then del the key "dob" then add new key "date_of_birth" and assign new key

# create another dict and name the variable as d2 and add info like "address", "street", "house_no"
# merge d1 and d2
